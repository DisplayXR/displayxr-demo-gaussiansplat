// Copyright 2026, The DisplayXR Project and its contributors
// SPDX-License-Identifier: Apache-2.0
//
// Gradle settings for the Android leg of displayxr-demo-gaussiansplat. The
// windows/ + macos/ legs build via the top-level CMakeLists.txt; the android/
// leg is an Android Gradle application that consumes the shared 3dgs_common/
// renderer sources by relative path (see android/src/main/cpp/CMakeLists.txt).

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'displayxr-demo-gaussiansplat'

include ':android'
project(':android').projectDir = new File(rootDir, 'android')
